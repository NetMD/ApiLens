Phase G2 — v0.1.0 Release tag (2026-06-03 NAS dogfooding 반영 재개 · 경량 실행 스펙)

## 정체성

CLAUDE.md 먼저 읽고 시작. 본 문서는 `prompt/20260526_01-2_Phase G2 — v0.1.0 Release tag (...).md` 의 **갱신·대체본**이다.

5/26 G2 프롬프트는 두 가지 핵심 가정 위에 쓰였다:
1. "G3 NAS dogfooding 이미 PASS 확정" (vams 측 mp4 사이즈 보존 입증)
2. "R12 = 코드 변경 0 라운드 (docs / CHANGELOG / git tag 만)"

→ 이 두 가정은 **2026-06-03 NAS 원격(app≠server) dogfooding** 으로 깨졌다. NAS 에 실제 부착했더니 co-located 환경이 구조적으로 가리던 결함이 추가로 노출됐고, **코드 변경을 동반**했다 (inter-pipeline-2026-06-03T20-28-30 영구 기록):

- **★P0★ (R8)** `CapturedResultSet.convertValue` NULL 기본값이 반환 primitive 타입과 불일치 → `getLong` JDK Proxy 언박싱 ClassCast → 호스트 앱 기동 다운루프
- **★P0 최severe★ (R10)** capture-result-set 가 호스트 SELECT 결과를 100행 cap 에서 truncate + 원본 조기 close → HikariCP 누수 / 롤백. 설계를 preread-close-proxy → **pass-through tee** 로 교체
- **★출시차단★ (R5)** wizard 가 agent 실제 키와 다른 `-D` 키 3건 생성 → agent 가 localhost fallback → 원격 trace 0건
- 그 외 R1(edge-triggered 로깅) / R3(agent-jar 다운로드 endpoint) / R4(routeSearch step 누수) / R6→R7(실행환경 탭) / R9(LatencyScatter 고정 윈도우) / R2(docs/api.md W-01 정정) — 총 27개 파일 수정

이 위험 작업은 **이미 완료·코드 반영·NAS 재배포 검증까지 끝났다** (workflow 코드 대조 6/6 PASS). 따라서 남은 일은 릴리스 **패키징**(CHANGELOG 기록 / 버전 unmark / docs 마무리 / git tag) 뿐이라 mechanical 하다.

→ **본 라운드는 정식 8-stage /pipeline 이 아니라 경량 릴리스 실행 스펙이다.** PM/Planner/Architect/Dev/QA/Security/review-arch/review-plan/release-plan 풀 코스를 돌릴 규모가 아님. EXT 누적 / agent 강화 / 패턴 표준화 등 파이프라인 내부 부기는 본 라운드 범위 밖(v0.1.1 이연).

## 승인된 범위 (사용자 결정)

**DO NOW (이번 릴리스에 포함):**
- **Tier A** — 진짜 출시 블로커 전부
- **B1~B3** — 6/3 dogfooding 이 추가한 신규 비협상 3건 release-checklist 정식 등재
- **가벼운 B4** — wizard 키 3건 4곳(docs SSOT / AgentConfig / FE / BE) 정합 grep 검증
- **progress.md** stale 갱신

**DEFER → v0.1.1 (이번엔 안 함):**
- **Tier C** — polish backlog (R11-R-01~07 / Playwright+MockTime / R9 잔존 / docs/troubleshooting.md 신규 / README "권장 가드" 절)
- **Tier D** — 파이프라인 내부 부기 (EXT 트리거 누적 9건 / planner EXT-001 Core 승급 결정 / PM·Architect agent 강화 / Pattern 표준화 / operating-rules.md)

> 단, Tier C 중 **WebFlux Known Limitation 한 줄**은 보안 관련성·비용 0 이라 CHANGELOG 기존 "Known limitations" 절에만 추가(예외 포함). docs/troubleshooting.md·README 권장 가드 절은 v0.1.1.

**사용자 손 필요 (Claude 불가):**
- **A1** — V-USER 10화면 시각 sign-off
- **A7** — git init + annotated tag (A1 sign-off 통과 후)

## 게이트 재조정 (5/26 G2 → 2026-06-03 현재)

| 게이트 | 5/26 G2 상태 | 현재(6/3 반영) 상태 |
|---|---|---|
| G1 — V-USER 10건 시각 sign-off | 일부 PASS + smoke #8/#9 시간의존 잔존 | **미완 (사용자 손, A1)** — 6/3 신규 화면(실행환경 탭/다운로드 버튼) 시각 확인 추가 권장 |
| G2 — W-01 lastSeen→lastSeenAt CHANGELOG BREAKING | 미기록 | docs/api.md 는 6/3 R2 로 이미 정정 ✅ / **CHANGELOG 기록만 미완** |
| G3 — NAS dogfooding 1회 | "이미 PASS 확정" | **6/3 NAS 원격으로 재수행 — 오히려 P0 2건+출시차단 발견·fix·재배포 검증 완료 ✅** (가정보다 강한 검증) |
| G3 보강#1 — mp4 사이즈 보존 | PASS (vams 2차) ✅ | 유지 ✅ |
| G3 보강#2 — shadowJar relocate javap | R12 재실행 예정 | **재실행 필요 (A8, 기계적)** — ResourceMixIn JsonIgnore 5 hit / raw 0 hit |
| G8 — R11 P0 truncate CHANGELOG SECURITY | 미기록 | **CHANGELOG 기록 미완** + **6/3 P0 2건도 같이 기록 필요(신규)** |
| 신규 B1 — wizard 키 ↔ AgentConfig/docs 대조 | 없음(가림막) | **코드 fix 완료, release-checklist 등재 미완** |
| 신규 B2 — 원격(app≠server) dogfooding 1회 | 없음 | **6/3 수행 완료, 게이트 정식 등재 미완** |
| 신규 B3 — capture-result-set host-data-integrity(행 무손실/lifecycle) | 없음 | **tee 코드 fix·검증 완료, 등재 미완** |

## 실행 항목 (Claude 직접 — 전부 working-tree 편집, git 없으므로 모두 reversible)

1. **CHANGELOG.md** `[0.1.0]` 절 (첫 release 이므로 [Unreleased] 가 아니라 [0.1.0] 으로 통합):
   - 날짜 `2026-XX-XX` → 실제 tag 날짜 (사용자 tag 시점에 최종 확정)
   - `### Changed` (현재 N/A) → **SNAPSHOT 대비 breaking 2건**:
     - W-01: `GET /v1/services` 응답 `lastSeen`(항상 값) → `lastSeenAt`(nullable) + `registeredAt`/`source`/`healthStatus` 추가
     - R5: wizard `-D` 키 3건 교정 — `apilens.server.url`→`apilens.server`, `apilens.capture.params`→`apilens.jdbc.capture-params`, `apilens.capture.resultset`→`apilens.jdbc.capture-result-set`. 옛 SNAPSHOT wizard 출력 복붙 환경은 키 교정 필요(자동 이관 없음)
     - 두 건 모두 "0.1.0 이 첫 public release → 공개 버전 간 호환성 영향 0, 단 사전 SNAPSHOT 배포 환경 한정 즉시 재배포 권장" 명문
   - `### Fixed` (현재 N/A) → **R8 P0**: capture-result-set NULL primitive getter ClassCast 호스트 크래시 차단 (반환 primitive 타입별 정합 wrapper)
   - `### Security` → **P0 데이터 무결성 2종 prepend** (기존 Apache 라이선스/capture-params 항목 보존):
     - (CWE-440/664/OWASP A08) agent serialize 가 `FileSystemResource.getOutputStream()` 등으로 호스트 파일 0바이트 truncate → multi-layer 3 layer defense (isUnsafeToSerialize 11 타입 + unwrapResponseEntity + ResourceMixIn). vams 실측 mp4 649MB 손실 사례
     - capture-result-set 가 호스트 SELECT 결과 truncate + 커넥션 누수 → pass-through tee 재설계 (`truncated=true`=payload 샘플 상한, 호스트 손실 아님)
     - **이전 SNAPSHOT 빌드 즉시 0.1.0 재배포 권고**
   - `## Known limitations (v0.1)` 에 **WebFlux 한 줄** 추가 (isUnsafeToSerialize 11 타입에 WebFlux `FilePart` 미포함 / v0.1 은 WebFlux 미instrument)
2. **build.gradle.kts:7** `version = "0.1.0-SNAPSHOT"` → `"0.1.0"` (CLAUDE.md §9 unmark) — ⚠️ tag 와 동시성 위해 A1 sign-off 후 적용 권장하나, reversible 이라 미리 적용 후 sign-off 실패 시 원복 가능
3. **docs/api.md** `/v1/setup/agent-jar-path` + `/v1/setup/agent-jar` 의 "인증 없음" 노트를 ⚠️ 경고 블록으로 강화 — "setup endpoint 는 신뢰 네트워크에서만 노출, 외부 노출 금지 (v0.3 인증 phase 까지)" (A9 / Security Low #1)
4. **docs/progress.md** stale 갱신 — Phase F(UI)/H(wizard·health)/release-polish 까지 반영. ⚠️ 파일 상단 룰 준수: 수동 검증(V-USER sign-off) 항목은 "통과 확인됨" 자동 기록 금지 → pending 으로 둠
5. **B4 정합 검증** — `apilens.server` / `apilens.jdbc.capture-params` / `apilens.jdbc.capture-result-set` 3키가 docs/agent-options.md(SSOT) / AgentConfig.java PROP_* / agent-option-builder.ts / AgentOptionBuilder.java 4곳 모두 일치하는지 grep. 불일치 0 hit 확인 (자동 골든테스트 강화는 v0.1.1)
6. **B1~B3 등재** — `~/.claude/pmo/ApiLens/next-project-checklist.md` 의 release-gate 절에 신규 비협상 3건 추가:
   - wizard 출력 키 ↔ AgentConfig PROP_*/docs SSOT 대조 (parity 만으론 양쪽 똑같이 틀림)
   - 원격(app≠server) dogfooding 1회
   - capture-result-set host-data-integrity (행 무손실 / lifecycle 위임) 검증

## 인계 항목 (사용자 수행 — "수동 검증 통과 확인됨" 자동 단정 금지)

- **A1 — V-USER 10화면 시각 sign-off** (smoke #2/#3/#6/#8/#9 + V-USER-R10-01~05). 6/3 신규 화면(Setup Step4 실행환경 탭 java/maven/gradle/docker + agent-jar 다운로드 버튼 / LatencyScatter 1m·5m 프리셋·고정 윈도우) 시각 확인 추가 권장. smoke #8(5분 stale)/#9(30분 inactive)는 시간 의존.
- **A8 빌드 검증** — `JAVA_HOME=21 ./gradlew clean build` 전체 PASS(회귀 0) + shadowJar relocate javap 재검증(relocated 5 hit / raw 0 hit). Claude 가 환경에서 시도하되 실패 시 사용자 환경 안내.
- **A7 — git init + annotated tag** (A1 sign-off 통과 후, 사용자 확인 하에):
  - 현재 디렉터리는 git repo 가 아님 → `git init` 필요 결정
  - `git tag -a v0.1.0 -m "..."` (4요소: 버전 / 날짜 / 본질 의도 "도입 사이클 명령어 조립 부담 0" + P0 안전성 / breaking 1줄 — wizard 키 + lastSeenAt)
  - tag 직후 vams 측에 "v0.1.0 정식 버전" 알림 → vams pom.xml 가드 코멘트 동기화 트리거 (5/26 G2 §vams 후속 sync 의무 유지)

## 보존 / 주의 (비협상)

- CLAUDE.md "절대 변경하지 말아야 할 결정 사항" 9항목 (특히 §9 버전 라벨) 준수
- v0.1 기능 11항목 / 제외 8종 / 회귀 가드 R15·R6·R14 / grep `payloads = ` 0 hit 단언 — 기존 CHANGELOG 보존 (재서술 금지)
- 6/3 dogfooding 의 코드 fix(CapturedResultSet tee / AdviceSupport 3-layer / AgentConfig 키 / HttpTransport edge-trigger / routeSearch / LatencyScatter)는 **변경 0** — 본 라운드는 문서/버전/태그만
- agent 호스트 영향 0 원칙 (R6 try-catch silent drop + 위험 타입 사전 차단 + tee) 유지
- 위험 작업은 6/3 에서 끝남 → 본 라운드에서 신규 코드/설계 변경 금지 (CHANGELOG/docs/version/checklist 외)

## 참고 읽기 (작업 시작 전)

- CLAUDE.md
- 기존 CHANGELOG.md / docs/api.md / docs/progress.md (보존 대상)
- prompt/20260526_01-2 Phase G2 (대체 대상, 컨텍스트 비교용)
- ~/.claude/pmo/ApiLens/inter-pipeline-2026-06-03T20-28-30_*.md (6/3 NAS dogfooding 10 라운드)
- ~/.claude/pmo/ApiLens/inter-pipeline-2026-05-26T06-04-22_retrospective-handoff.md (R11→R12 게이트 정의)
- ~/.claude/pmo/ApiLens/next-project-checklist.md (release-gate 절 — B1~B3 등재 대상)
