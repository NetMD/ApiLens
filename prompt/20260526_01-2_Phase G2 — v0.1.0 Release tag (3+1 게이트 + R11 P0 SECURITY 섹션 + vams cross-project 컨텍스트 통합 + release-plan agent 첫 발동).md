Phase G2 — v0.1.0 Release tag (3+1 게이트 + R11 P0 SECURITY 섹션 + vams cross-project 컨텍스트 통합 + release-plan agent 첫 발동)

## 컨텍스트

CLAUDE.md 먼저 읽고 시작. R11 Phase H 후속 #2 (agent serializer P0 file truncate fix, 2026-05-26 06:18 종료) 직후 vams 측 외부 사용자 dogfooding 으로 두 번째 release blocker 해제 입증 + 8 카테고리 cross-project 컨텍스트 회신 받음 (inter-pipeline-2026-05-26T06-51-02 영구 기록).

본 R12 는 **v0.1.0 release tag 생성 직접 phase**. R10 (회수 6건) + R11 (P0 BUG fix) 의 release blocker 모두 해제 완료. R12 = 마지막 phase, 3+1 게이트 + CHANGELOG + git tag annotated + docs polish + vams 측 컨텍스트 흡수 후 release tag 생성.

**역사적 맥락**:
- Phase A~F2 = v0.1 핵심 기능 구현 (R0~R8)
- Phase G1 = Release polish (1) — docs + 라이선스 + CI (R7, 2026-05-15)
- Phase H = Setup wizard + Active services + Per-service health 도입 친화성 (R9, 2026-05-22)
- R10 = Phase H 후속 (dogfooding 6건 회수, 2026-05-26 01:35)
- R11 = Phase H 후속 #2 (P0 agent file truncate fix, 2026-05-26 06:18)
- **R12 = Phase G2 v0.1.0 Release tag 생성 — 본 라운드**

## v0.1.0 release tag 직전 3+1 게이트 (비협상, 변경 금지)

R10/R11 retrospective handoff §2 의제 5 (release tag 직전 3 게이트) 비협상 + R11 회수에서 추가된 G8 신규 게이트.

### G1 — V-USER 시각 검증 합산 10건 + R11 신규 5

R9 Phase H smoke #2/#3/#6/#8/#9 5건 + R10 V-USER-R10-01~05 5건 + R11 신규 V-USER (해당 시) = 사용자 본인 환경 sign-off

각 V-USER 항목별 phase 진입 시점 sign-off 받은 결정값이 release tag 직전 docs 박제 여부만 자동 grep 검증 — **재검토 금지** (EXT-005 절차 3).

**이미 PASS 항목** (R10/R11 dogfooding 검증):
- ✅ wizard 4단계 시각 톤 (R10 V-USER-R10-01~04 + R10 dogfooding)
- ✅ ActiveServices health dot 신호등 톤 (V-USER-R10-03)
- ✅ dashboard service 자동 selection (V-USER-R10-05)
- ✅ agent jar 자동 추출 + JVM 옵션 절대경로 박힘 (V-USER-R10-04 + R10/R11 dogfooding)
- ✅ smoke #2/#3/#6 wizard 정상 동작 (사용자 R10 dogfooding)

**별도 시각 게이트** (V-USER-1 smoke #8/#9 시간 의존):
- [ ] smoke #8 5분 후 ActiveServices dot 🟢→🟡 stale 전이
- [ ] smoke #9 30분 후 🟡→🔴 inactive 전이
- [ ] (선택) 탭 복귀 시 자동 갱신 (refetchOnWindowFocus: true)

### G2 — W-01 lastSeen → lastSeenAt CHANGELOG BREAKING CHANGES 섹션 기록

v0.1.0 미공개 release 가정 (deprecation period 0) 라도 명문 기록 의무. `lastSeen` 필드명을 모든 API 응답에서 `lastSeenAt` 으로 단일화 (R9 Phase H 결정).

### G3 — NAS dogfooding 1회 (★ 이미 PASS 확정 ★)

Synology NAS 또는 동등 환경 + sample-app/VAMS + 첫 trace 5분 내 dashboard 관측. release blocker 차단.

**PASS 확정**:
- ✅ ApiLens 측 R10 sample-app dogfooding (2026-05-25)
- ✅ vams 측 R11 사후 검증 — agent jar 신빌드 (12,236,593B, 2026-05-26 06:24) 부착 후 `/api/videos/613/stream` 06:25:32 / 06:26:05 두 차례 모두 mp4 사이즈 보존
- 0바이트로 잘렸던 video 611 (145MB) + video 613 (305MB) + video 616 (199MB) = 649MB 모두 사용자 백업본으로 복원 완료
- vams 측 회귀 게이트: **CLOSED**

본 R12 에서는 G3 의 자동 검증만 재확인 (별도 dogfooding 재실행 X — 충분 입증)

### G8 — R11 P0 BUG fix CHANGELOG `## SECURITY` 섹션 분리 (★ R11 신규 ★)

R11 P0 fix 가 CWE-440 / CWE-664 / OWASP A08 critical security issue 회수에 해당. v0.1.0 미공개 release 라도 운영자 즉시 인지 필수 — `## SECURITY` 섹션 분리 박제 의무.

vams 측 §2 데이터 손실 649MB 실측 + §6 영향 범위 6 타입 + "0.6.x 이하 (= 본 fix 이전 SNAPSHOT 빌드) 사용 금지 권고" 모두 명문 흡수.

## vams 측 cross-project 컨텍스트 통합 (★ release tag 직접 영향 ★)

R11 종료 후 vams 측 claude-code 가 전달한 8 카테고리 컨텍스트 (inter-pipeline-2026-05-26T06-51-02 영구 기록) — 본 R12 에서 다음과 같이 흡수:

### vams §1 진단 시간 1시간+ / 잘못된 추측 3회 → accumulated-insights 보강

R12 STEP 17 회고에서 "**코드만 보면 절대 못 찾는 회귀**" 함정 카테고리를 누적 인사이트로 영구 보존. javaagent / aspect / bytecode instrumentation 계열 도구 진단의 범용 best practice (vams 측 동의 확정).

### vams §3 진단 SOP → docs/troubleshooting.md 신규 작성

ApiLens 신규 docs `docs/troubleshooting.md` 작성:
- 절 1: "agent 부착 후 호스트 앱 파일이 잘리는 의심 증상" — fs_usage `_WC_T` 추적
- 절 2: "agent 부착 후 trace 누락 의심" — JDBC capture 분기 점검
- 절 3: "agent 부착 후 호스트 앱 timeout/throughput 회귀 의심"
- 절 4: 진단 SOP 표 (stat mtime / agent ON/OFF / fs_usage 등)

### vams §6 회귀 가능성 영역 + WebFlux FilePart 별도 가드 → CHANGELOG `## Known Limitations`

vams §6 식별 6 응용 케이스 ApiLens v0.1.0 적용 상태:
- ✅ `FileSystemResource` (R11 fix 입증)
- ✅ `PathResource` (R11 isUnsafeToSerialize 11 타입 포함, startsWith `org.springframework.core.io.`)
- ✅ `FileUrlResource` (R11 동일 prefix)
- ✅ `ResourceRegion` (R11 fix 입증)
- ✅ `MultipartFile.transferTo` (R11 isUnsafeToSerialize 11 타입 포함, startsWith `org.springframework.web.multipart.`)
- ✅ `StreamingResponseBody` (R11 isUnsafeToSerialize 11 타입 포함, contains 매칭)
- ⚠️ `org.springframework.http.codec.multipart.FilePart` (WebFlux 영역, v0.1 isUnsafeToSerialize 11 타입에 미포함)

CHANGELOG `## Known Limitations` 신규 섹션 등재:
```
- WebFlux 기반 앱 (Spring WebFlux + Reactor) 사용 시 별도 가드 필요. v0.1 의
  isUnsafeToSerialize 위험 타입 11종에 FilePart 등 WebFlux 전용 타입 미포함.
  ThreadLocal 기반 trace context 한계로 v0.1 은 WebFlux 자체 instrument 안 됨 (직접
  영향 0 추정), v0.2+ WebFlux 지원 시 추가 검토 필요.
```

vams 는 servlet stack (Spring MVC + Tomcat) 운영이라 영향 0 확정 (vams §6 회신).

### vams §8 pom.xml 가드 코멘트 sample → README "ApiLens agent 부착 시 권장 가드" 신규 절

vams 허락 받음 (vams 회신 §8). README 신규 절 "Spring Boot Maven plugin jvmArguments 가드 코멘트 sample 템플릿":

**조건 (vams 허락 조건)**:
- 사용자 절대경로 `/Users/netmd/.apilens/apilens-agent.jar` 는 일반화된 sample path (`${user.home}/.apilens/apilens-agent.jar` 또는 `/path/to/apilens-agent.jar`) 로 교체
- 나머지 가드 코멘트 본문은 그대로 인용 가능
- 출처 표기 ApiLens 측 재량 — 표기 시 "vams (analysis) 프로젝트 현장 적용 사례 기반" 정도

### vams 후속 sync 의무 (release tag 시점)

vams pom.xml 가드 코멘트 현재: "해결: apilens-agent.jar 2026-05-26 06:24 이후 빌드에 ..."
→ vams 측 v0.1.0 release tag 확정 후 "해결: apilens-agent v0.1.0 이상에 ..." 로 업데이트 예정

**ApiLens 측 의무**: R12 종료 + release tag 생성 직후 사용자에게 "v0.1.0 정식 버전 번호 + tag stamp 알림" 명문 안내 → 사용자가 vams 측에 동기화 트리거.

## release-plan agent 첫 발동 (★ R10 STEP 1 S-27 신규 생성, 본 R12 가 첫 발동 ★)

`~/.claude/agents/release-plan.md` (R10 STEP 1 신규 생성) 가 본 R12 에서 첫 발동.

release-plan agent 의 3 책임 단일화:
1. **CHANGELOG breaking 섹션 + 분류 표 정합** — `## [v0.1.0] - 2026-05-XX` + `### Added` (R9 Phase H setup wizard + ActiveServices + health 등) + `### Changed` (W-01 BREAKING lastSeen → lastSeenAt) + `### Security` (R11 P0 file truncate fix critical) + `### Known Limitations` (WebFlux v0.1 별도 가드)
2. **git tag annotated message 4 요소** — version / date / highlights / 호환성 메모
3. **잔여 backlog 3 카테고리 분류 표** — v0.1.1 polish / v0.2 features / v0.3 인증

## STEP 1 자동 인계 의제 (사용자 결정 게이트)

R11 회고 §8 + R10 핸드오프 §5 명문:

### 1. planner.ApiLens EXT-001 5/5 PROMOTE_CANDIDATE Core 영구화 결정 게이트 (사용자 직접 결정 필요)

planner.ApiLens.ext.md EXT-001 (사전 grep 검증 표) 가 R6 → F2 fix³ → E3 → G1 → R10 → R11 (5회차) 도달. STEP 1 모드 B 사용자 결정:
- 옵션 A: Core 영구화 (planner.md 본 정의에 통합) — 모든 프로젝트에 default 적용
- 옵션 B: ApiLens 전용 유지 (현재 상태)
- 옵션 C: ARCHIVE (트리거 측정 중단, 본문 보존)

권장: **A Core 영구화** (5회차 누적 + 두 프로젝트 (ApiLens / analysis) 검증 + 매 라운드 효과 입증).

### 2. retrospective.ext EXT-002 (S-32 R11 신규) 두 번째 발동

R11 회고 §9 첫 발동 → R12 (release-gate phase) 진입 직전 두 번째 발동 자연 예상. EXT 4 카테고리 사전 점검 표 (트리거 누적 / Core 승급 / INACTIVE / 흡수) 박제.

### 3. release-plan agent (R10 STEP 1 S-27 신규) Core 첫 적용

본 R12 가 release-plan agent 첫 사용 phase. STEP 13 또는 별 stage 로 호출 (PM/Planner/Architect/Dev/QA/Sec/Review-arch/Review-plan 완료 후 retrospective 직전, 또는 review-plan 직후).

## 작업 범위 (Backend 0 + Frontend 0 + docs 다수)

**v0.1.0 release tag = 코드 변경 0 라운드**. 본 R12 는 docs / CHANGELOG / README / 운영 검증 / git tag 생성만.

### 변경 파일 추정 (architect 가 ground truth grep 확정)

- `CHANGELOG.md` (변경) — `## [v0.1.0] - 2026-05-26` 항목 추가
  - `### Added` (R9 Phase H 신규 기능)
  - `### Changed` (W-01 BREAKING lastSeen → lastSeenAt)
  - `### Security` (R11 P0 file truncate fix critical, vams §2 데이터 손실 649MB + §6 영향 범위 + "0.6.x 이하 사용 금지 권고")
  - `### Known Limitations` (vams §6 WebFlux FilePart 별도 가드)
- `README.md` (변경) — "ApiLens agent 부착 시 권장 가드" 신규 절 (vams 허락 sample, 절대경로 일반화)
- `docs/troubleshooting.md` (신규) — vams §3 진단 SOP + agent 부착 후 의심 증상 분류 + fs_usage `_WC_T` 추적
- `docs/api.md` (변경) — `lastSeen` → `lastSeenAt` 단일화 명문 (W-01 정합)
- `apilens-server/.../setup/AgentJarPathResponse.java` 등 (확인만) — R10/R11 BE 변경 0 line 확정
- (선택) `git tag -a v0.1.0 -m "..."` 명령 실행 직전 메시지 4 요소 준비 (release-plan agent 가 작성)

### 검증 의무 (사용자가 수행)

자동:
1. **빌드 전체 검증**: `JAVA_HOME=21 ./gradlew clean build` PASS — agent shadowJar + server bootJar + UI 빌드 + 전체 247+ tests 회귀 0
2. **CHANGELOG / README / docs/troubleshooting 정합 grep** — W-01 `lastSeen` 잔존 0 (W-01 SECURITY 섹션 등재값 외) + vams §6 6 타입 명문 + WebFlux limitation 명문
3. **release-plan agent 산출물** — CHANGELOG `## [v0.1.0]` 4 섹션 (Added/Changed/Security/Known Limitations) 모두 충족 + git tag message 4 요소 + backlog 3 카테고리 분류 표
4. **재현 가능 빌드 검증** — `git clean -fdx && git checkout v0.1.0 && JAVA_HOME=21 ./gradlew build` 재현 가능성

수동:
- [ ] 사용자 git tag 생성 시점 알림 → vams 측에 v0.1.0 정식 버전 번호 + stamp 동기화 트리거
- [ ] V-USER smoke #8 (5분) / #9 (30분) 시간 의존 검증 (PM §6 결정)

## R12 라운드 정체성 선언 (PM §0 EXT-003 적용 의무)

- **v0.1.0 release tag 생성 phase** — 코드 변경 0 라운드, docs / CHANGELOG / README / git tag 만
- R9 Phase H + R10 회수 + R11 P0 fix 모든 결정 보존 (D-01~D-05 / W-01~W-03 / V-USER-H1·H2 / Q-01~Q-08 / SH-* / D-H10-01~06 / D-P0-01~05)
- vams 측 cross-project 컨텍스트 8 카테고리 흡수 완료 (조건 1건 — pom.xml sample 절대경로 일반화)
- R11 P0 fix 가 CWE-440 / CWE-664 / OWASP A08 critical security issue 회수 — CHANGELOG `## SECURITY` 섹션 분리 명문 (G8)
- WebFlux FilePart limitation 별도 가드 — CHANGELOG `## Known Limitations` 신규 섹션 (vams §6 회신)
- 본 R12 = 마지막 phase before v0.1.0 tag. R13+ 는 v0.1.1 polish 또는 v0.2 features 영역

## docs/CHANGELOG 표준 구조 (release-plan agent 가 작성)

```markdown
## [v0.1.0] - 2026-05-26

### Added (R9 Phase H 신규 기능 — Setup wizard + Active services + Per-service health)
- Setup wizard 4단계 (`/setup`) — Server URL default 자동 입력, Service Name 안내 운영자 어휘,
  Capture Options, JVM 옵션 절대경로 박힘 (agent jar 자동 추출 via `GET /v1/setup/agent-jar-path`)
- ActiveServices 페이지 (`/services`) — health 신호등 dot (green/amber/red/stone) + 30s polling +
  refetchOnWindowFocus
- Dashboard service 자동 selection (wizard 완료 + services 1건만 자동)
- SPA 라우팅 fallback (WebMvcConfig, R10 inter-pipeline)
- 신규 API 4건 (POST /v1/setup/complete, GET /v1/setup/state, GET /v1/setup/agent-jar-path,
  PATCH /v1/services/{name})

### Changed
- **BREAKING** `lastSeen` 필드 → `lastSeenAt` 으로 단일화 (모든 services API 응답).
  v0.1.0 미공개 release 라 deprecation 없음. v0.1.0 이전 SNAPSHOT 사용자는 client 측
  `lastSeen` 참조를 `lastSeenAt` 으로 즉시 업데이트 필요

### Security
- **[CRITICAL]** agent serializer P0 file truncate 회수 — `AdviceSupport.serializeReturn` 이
  `ResponseEntity<ResourceRegion>` body 를 Jackson 으로 직렬화 시 `FileSystemResource.getOutputStream()`
  호출 → `Files.newOutputStream(WRITE | CREATE | TRUNCATE_EXISTING)` → 호스트 앱 파일이 0바이트로
  잘림. CWE-440 / CWE-664 / OWASP A08 critical security issue.
- 영향 범위: `apilens-agent` instrument 대상 모든 Spring 앱의 `Resource` / `ResourceRegion` /
  `MultipartFile.transferTo` / `StreamingResponseBody` 반환 endpoint 전체
- 실측 데이터 손실 사례: vams (analysis) 프로젝트 비디오 스트리밍 endpoint 호출 시 mp4 파일
  649MB (3개 파일) 0바이트 truncate. 백업본으로 복원 완료
- multi-layer 3 layer defense 적용: Layer 1 (`isUnsafeToSerialize` helper, 위험 타입 11종 사전 skip)
  + Layer 2 (`unwrapResponseEntity` 보강) + Layer 3 (Jackson MAPPER `ResourceMixIn` reflection 등록)
- shadow jar relocate 정합 bytecode `javap -v` 직접 검증 (relocated 5 hit / raw 0 hit)
- **v0.1.0 이전 SNAPSHOT 빌드 사용 금지 권고** — `apilens-agent.jar` v0.1.0 이상으로 즉시 교체

### Known Limitations (v0.1 한계, v0.2+ 후속)
- WebFlux 기반 앱 별도 가드 필요 — v0.1 `isUnsafeToSerialize` 위험 타입 11종에 WebFlux 전용 타입
  (`FilePart` 등) 미포함. v0.1 은 ThreadLocal 기반 trace context 한계로 WebFlux 자체 instrument
  안 됨 (직접 영향 0 추정), v0.2+ 추가 검토 예정
- JDBC SQL 캡처 PreparedStatement 만 지원 (raw Statement v0.2)
- W3C `traceparent` 인커밍 헤더 무시 (MSA propagation v0.3)
- Servlet body raw bytes 미캡처 (controller args/return JSON 직렬화만, v0.2)
- client-side 마스킹 비활성 (server-side 만, v0.2 토글 옵션)
```

## git tag annotated message 4 요소 (release-plan agent 작성)

```
v0.1.0 — 한국 SI 운영자용 Spring Boot 호출 추적 도구 (initial release)

Highlights:
- Single jar 배포 (agent + collector + storage + UI 통합)
- Setup wizard 4단계 — 명령어 조립 부담 0 (vams 측 dogfooding 입증)
- Server-side 마스킹 라이브 프리뷰 (운영자 결재 친화)
- multi-layer 3 layer defense agent 안전성 (R11 P0 fix 적용)

호환성:
- Java 21 (HOST 앱), agent jar 는 host JVM 의 class file major version 따름
- ByteBuddy 1.17.8 (Java 26 정식 지원)
- Spring Boot 호스트 앱 instrument (WebFlux 미지원)
- License: Apache 2.0 (donation only, 무료 OSS)
```

## QA / Security / review-arch / review-plan / release-plan 의무

### QA
- 전체 빌드 `JAVA_HOME=21 ./gradlew clean build` PASS — 247+ tests 회귀 0
- CHANGELOG 4 섹션 정합 grep (Added / Changed / Security / Known Limitations 모두 hit)
- README "권장 가드" 신규 절 + 절대경로 일반화 검증 (`/Users/netmd/.apilens` 잔존 0)
- docs/troubleshooting.md 신규 + vams §3 SOP 정합
- W-01 `lastSeen` 잔존 0 (Security 섹션 등재값 외)
- vams §6 6 타입 명문 + WebFlux limitation 명문

### Security
- CHANGELOG `## SECURITY` 섹션 CWE-440 / CWE-664 / OWASP A08 명문 정합
- 데이터 손실 649MB 실측 + 영향 범위 + "0.6.x 이하 사용 금지 권고" 모두 명문
- WebFlux limitation v0.1 한계 명문 (v0.2+ 추가 검토 예정)
- 본 R12 변경 0 (docs only) — 신규 attack surface 0

### review-arch (자기증명 grep 9축, S-25 EXT-003 세 번째 트리거)
- 8 기존 축 (anchor verbatim / Plan AC / 비협상 라벨 / 정방향 동사 / 반대방향 0 hit / Q-08 parity / Ground truth 정정 / R9/R10/R11 본체 회귀 0)
- 9번째 축 (잘못된 lock-in 0 hit) — release tag 생성 직전 critical issue 우회 lock-in 0

### review-plan (G1-C 4 출처 release blocker 0 누적 표, EXT-001 네 번째 발동)
- Planner self-증명: 본질 의도 (release tag 생성 + vams 컨텍스트 흡수) 모든 docs 정합
- QA: 빌드 게이트 + CHANGELOG/README/docs 정합 + W-01 일관성
- Security: CHANGELOG SECURITY 명문 + Known Limitations
- Review-arch: 9축 grep + ground truth 정정

### release-plan agent (R10 신규 S-27, 첫 발동) ★
- CHANGELOG `## [v0.1.0]` 4 섹션 작성
- git tag annotated message 4 요소 작성
- 잔여 backlog 3 카테고리 분류 표 작성 (v0.1.1 polish / v0.2 features / v0.3 인증)

## 사용자 검증 (R12 종료 + git tag 생성 직후)

자동:
1. `JAVA_HOME=21 ./gradlew clean build` PASS
2. `git tag -a v0.1.0 -m "..."` (release-plan 작성한 message 사용)
3. `git tag --verify v0.1.0` (annotated tag 정상 생성 확인)
4. (선택) `git push origin v0.1.0`

수동:
- [ ] CHANGELOG `## [v0.1.0]` 4 섹션 본인 눈으로 확인
- [ ] README "권장 가드" 신규 절 본인 눈으로 확인
- [ ] docs/troubleshooting.md 신규 본인 눈으로 확인
- [ ] V-USER smoke #8 (5분) / #9 (30분) 시간 의존 검증 (선택)
- [ ] vams 측에 v0.1.0 정식 버전 번호 알림 + vams pom.xml 가드 코멘트 동기화 트리거

## 주의

- 본 R12 사용자 비협상 4 게이트 (G1/G2/G3/G8) verbatim 봉인 — architect 재해석 금지
- R9/R10/R11 모든 결정 보존 (변경 0 line)
- vams 측 cross-project 컨텍스트 8 카테고리 흡수 의무 (특히 §1 / §3 / §6 / §8)
- vams 허락 조건 (sample 절대경로 일반화) 의무 정합
- 본 R12 = 코드 변경 0 라운드 — apilens-server / apilens-agent / apilens-ui 모듈 build.gradle.kts 외 변경 금지 (의존성 추가/제거 0)
- TypeScript / FE 변경 0
- WebFlux FilePart 미포함 명문 (Known Limitations 명문 박제 의무)
- "수동 검증 통과 확인됨" 단정 보고 금지 — 사용자 검증 항목 명시
- R12 종료 + git tag 생성 직후 사용자 알림에 vams 측 동기화 트리거 안내 명문

작업 시작 전 (release-plan / docs 작업 시):
- CLAUDE.md (특히 "Agent 가볍게" + "마스킹 server-side" + "단일 jar 배포")
- inter-pipeline-2026-05-26T06-51-02_*.md (vams 측 cross-project 컨텍스트 8 카테고리)
- R11 PMO 산출물 12건 (~/.claude/pmo/ApiLens/R11/)
- R10 PMO 산출물 14건 (~/.claude/pmo/ApiLens/R10/)
- R9 Phase H notification (~/.claude/pmo/ApiLens/R9/2026-05-22_23-53_notification_*.md)
- 기존 CHANGELOG.md (Phase G1 release polish 결과 보존)
- 기존 README.md (Phase G1 docs polish 결과 보존)
- ~/.claude/agents/release-plan.md (R10 신규, 본 R12 첫 발동)
